# Demo: полный аудит медицинского офиса

Это обезличенный фактический запуск `audit_med_20260717`, выполненный 17–18 июля 2026 года с версией скилла `0.7.5`. Публикуемый payload имеет версию `0.7.7`, поэтому пример является доказательством реального workflow, а не golden test побайтового совпадения.

## Вход

- [`plan.png`](input/plan.png) — план пяти помещений;
- [`estimate.xlsx`](input/estimate.xlsx) — смета на 35 строк;
- [`site-photos.zip`](input/site-photos.zip) — архив из двух фотографий;
- `input/site-photos/` — те же фотографии отдельно для просмотра на GitHub.

Исходные материалы не содержат ФИО, адреса или контактных данных. Имена загрузок, локальные пути, task IDs и delegation tokens не включены в demo metadata.

## Как повторить

1. Установить скилл и запустить MCP по корневому `INSTALL.md`.
2. В новом диалоге загрузить `plan.png` и `estimate.xlsx`.
3. Попросить провести предварительный аудит сметы по плану.
4. Проверить geometry review. В фактическом запуске пользователь указал высоту всех помещений `2,8 м`, после чего была создана revision 2.
5. Отдельным сообщением подтвердить revision.
6. После deterministic audit загрузить `site-photos.zip`.
7. Дождаться Photo Vision, Analyst и формирования HTML.
8. После отчёта принять предложение скилла создать эталонную (контрольную) XLSX-смету.

## Фактический результат

| Метрика | Значение |
|---|---:|
| Помещений | 5 |
| Площадь пола | 164,36 м² |
| Уникальных дверей / окон | 6 / 5 |
| Строк сметы | 35 |
| Coverage количества | 100% |
| Coverage цены | 100% |
| Findings | 6 |
| Высокой важности / предупреждений | 2 / 4 |
| Строк с quantity deviation | 4 |
| Строк с price deviation | 2 |
| Фотографий / наблюдений | 2 / 16 |
| Аналитических гипотез | 4 |

Среди результатов были выявлены возможный дубликат установки окна, три расхождения количества и два расхождения цены. Фото-анализ отметил видимый дефект стыка напольного покрытия и явно отделил невидимые в кадре работы от подтверждённых наблюдений.

Машиночитаемая безопасная сводка: [`result-summary.json`](result-summary.json).

## Готовые артефакты

В [`output/`](output/) включены все 18 файлов фактического output-каталога: автономный HTML, контрольная XLSX, нормализованная смета, geometry и corrections, каталог цен, mapping, количества, трассировка расчётов, price checks, findings, photo workflow и analyst workflow.

Назначение каждого файла и порядок их появления описаны в [`ARTIFACTS.md`](ARTIFACTS.md).

HTML содержит исходные staged filenames и технические task IDs, сохранённые генератором отчёта для provenance. Он не содержит локальных абсолютных путей, delegation tokens или секретов.

Два JSON были минимально санитизированы для публичной публикации: в `mapping.json` заменён delegation token, а в `visual_photos.json` заменены два delegation token и два абсолютных локальных пути. Остальные output-файлы перенесены без изменений. Подробности и исходные SHA-256 указаны в [`SANITIZATION.md`](SANITIZATION.md).

## Ограничение интерпретации

Findings и гипотезы являются предварительными. Demo показывает работу конвейера и воспроизводимость расчётов, но не является заключением строительного эксперта.
