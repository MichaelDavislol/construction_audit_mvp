[project]
name = "price-mcp"
version = "0.1.0"
description = "Local MCP price catalog for Construction Audit MVP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.27,<2",
]
